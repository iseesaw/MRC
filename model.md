### Model of MRC Task
Bert + BiDAF + self-attention/match + Student/Teacher-model
(AoA or R-net\<Maybe have a try>)

- Data Preparing
    + query classifier
    + DA(MT & Q-A)
- embedding
    + BERT
    + pos-tag
    + other embedding methods
- encoding
- attention
- decoding